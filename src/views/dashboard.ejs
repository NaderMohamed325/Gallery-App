<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="flex items-center justify-center h-screen bg-gray-100">
    <div class="bg-white p-8 rounded-xl shadow-lg w-96">
      <h2 class="text-2xl font-bold text-center text-gray-700">Dashboard</h2>
      <p class="text-center text-gray-600 text-sm mt-4">Welcome to your dashboard!</p>
      <div class="mt-4">
        <form action="/api/v1/logout" method="POST">
          <button
            type="submit"
            class="w-full bg-red-500 text-white p-2 rounded mt-4 hover:bg-red-600 text-center block"
          >
            Logout
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
